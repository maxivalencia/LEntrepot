{% extends 'base4.html.twig' %}

{% block title %}Information Commentaire{% endblock %}

{% block contenue %}
    <h1>Information Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire.commentaire }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commentaire_index') }}">Retour</a>

    <a href="{{ path('commentaire_edit', {'id': commentaire.id}) }}">Modifier</a>

    {{ include('commentaire/_delete_form.html.twig') }}
{% endblock %}
