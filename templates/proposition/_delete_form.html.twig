<form method="post" action="{{ path('proposition_delete', {'id': proposition.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette proposition?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proposition.id) }}">
    <button class="btn">Supprimer</button>
</form>
