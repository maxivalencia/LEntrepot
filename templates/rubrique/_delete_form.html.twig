<form method="post" action="{{ path('rubrique_delete', {'id': rubrique.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce rubrique?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rubrique.id) }}">
    <button class="btn">Supprimer</button>
</form>
