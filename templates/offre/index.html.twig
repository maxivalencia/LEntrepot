{% extends 'base.html.twig' %}

{% block title %}{{ controller_name }}{% endblock %}

        {# {% block header %}
            {% include 'accueil/header.html.twig' %}
        {% endblock %} #}
{% block body %}
  <br>
  <br>
  <br>
  <br>
  <div><h2>{{ titre_rubrique }}</h2></div>

  <br>
  {# <div class="boxrubrique">
    <div class="contenuerubrique">exemple de texte</div>
  </div> #}
  <div class="container">
    <div class="row">
      {% for publication in publications %}  
        <div class="col-3">
          <div class="boxrubrique col-sm">
            {# <div class="titrerubrique"><u>{{ publication.user.username }}</u> : </div><br> #}
            <div><img src="/image/{{ publication.image }}" alt="image" width="200px"></div>
            <div class="contenuerubrique">{{ publication.publication|u.truncate(50) }} ...</div><br>
            <div class="lirelasuite"><a href="{{ path('details', {'id': publication.id}) }}">lire la suite ...</a></div>
          </div>
          <hr>
        </div>
      {% endfor %}
    </div>
  </div>

  <div class="navigation">
      {{ knp_pagination_render(publications) }}
  </div>
{% endblock %}
