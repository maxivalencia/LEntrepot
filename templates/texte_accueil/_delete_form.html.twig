<form method="post" action="{{ path('texte_accueil_delete', {'id': texte_accueil.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce texte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ texte_accueil.id) }}">
    <button class="btn">Supprimer</button>
</form>
