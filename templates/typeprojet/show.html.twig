{% extends 'base4.html.twig' %}

{% block title %}Information Type de projet{% endblock %}

{% block contenue %}
    <h1>Information Type de projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeprojet.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ typeprojet.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeprojet_index') }}">Retour</a>

    <a href="{{ path('typeprojet_edit', {'id': typeprojet.id}) }}">Modifier</a>

    {{ include('typeprojet/_delete_form.html.twig') }}
{% endblock %}
