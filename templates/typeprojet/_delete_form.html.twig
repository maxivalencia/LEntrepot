<form method="post" action="{{ path('typeprojet_delete', {'id': typeprojet.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeprojet.id) }}">
    <button class="btn">Delete</button>
</form>
