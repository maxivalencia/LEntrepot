{% extends 'base.html.twig' %}

{% block title %}ImagePublication{% endblock %}

{% block body %}
    <h1>ImagePublication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_publication.id }}</td>
            </tr>
            <tr>
                <th>NomFichier</th>
                <td>{{ image_publication.nomFichier }}</td>
            </tr>
            <tr>
                <th>NomServer</th>
                <td>{{ image_publication.nomServer }}</td>
            </tr>
            <tr>
                <th>ReferenceImage</th>
                <td>{{ image_publication.referenceImage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('image_publication_index') }}">back to list</a>

    <a href="{{ path('image_publication_edit', {'id': image_publication.id}) }}">edit</a>

    {{ include('image_publication/_delete_form.html.twig') }}
{% endblock %}
